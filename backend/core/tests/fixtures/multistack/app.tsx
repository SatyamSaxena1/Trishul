const child_process = require("child_process");

child_process.exec(input);
collection.find(req.body);
process.env.NODE_TLS_REJECT_UNAUTHORIZED = "0";
const uri = "mongodb://admin:password@database.example/test";

export const Unsafe = ({ value }) => <div dangerouslySetInnerHTML={{ __html: value }} />;
